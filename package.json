{
  "name": "lru-cache-persist",
  "version": "1.0.1",
  "description": "LRU cache with persist",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "rm -rf dist && tsc",
    "prepublish": "npm test",
    "test": "npm run build && jest --coverage"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@babel/preset-typescript": "^7.9.0",
    "@types/jest": "^25.1.4",
    "@types/node": "^13.9.5",
    "husky": "^4.2.3",
    "jest": "^25.2.3",
    "ts-jest": "^25.2.1",
    "ts-node-dev": "^1.0.0-pre.44",
    "tslint": "^6.1.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^3.8.3"
  }
}
